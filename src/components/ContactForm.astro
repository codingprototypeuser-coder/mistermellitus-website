---
// Wiederverwendbare Kontaktformular-Komponente
---

<div class="grid md:grid-cols-2 gap-12">
  <!-- Info -->
  <div>
    <h2 class="text-xl font-semibold mb-4">Lass uns in Kontakt bleiben</h2>
    <p class="text-[var(--text-secondary)] mb-6">
      Hast du Fragen, Anregungen oder möchtest einfach nur Hallo sagen? Ich freue mich über jede Nachricht!
    </p>

    <div class="space-y-4">
      <div class="flex items-start gap-4">
        <div class="w-10 h-10 rounded-lg bg-[var(--primary-light)]/20 flex items-center justify-center flex-shrink-0">
          <svg class="w-5 h-5 text-[var(--primary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
          </svg>
        </div>
        <div>
          <h3 class="font-medium">E-Mail</h3>
          <a href="mailto:info@mistermellitus.de" class="text-[var(--primary)] hover:text-[var(--primary-dark)]">
            info@mistermellitus.de
          </a>
        </div>
      </div>
    </div>

    <div class="mt-8 p-6 bg-[var(--bg-secondary)] rounded-xl">
      <h3 class="font-medium mb-2">Hinweis</h3>
      <p class="text-sm text-[var(--text-muted)]">
        Bitte beachte: Ich bin kein Arzt und kann keine medizinischen Ratschläge geben. Bei gesundheitlichen Fragen wende dich bitte an deinen Arzt oder dein Diabetes-Team.
      </p>
    </div>
  </div>

  <!-- Contact Form -->
  <div>
    <form class="space-y-6" action="#" method="POST">
      <div>
        <label for="name" class="block text-sm font-medium mb-2">Name</label>
        <input
          type="text"
          id="name"
          name="name"
          required
          class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:border-[var(--primary)] focus:ring-2 focus:ring-[var(--primary)]/20 outline-none transition-all"
          placeholder="Dein Name"
        />
      </div>

      <div>
        <label for="email" class="block text-sm font-medium mb-2">E-Mail</label>
        <input
          type="email"
          id="email"
          name="email"
          required
          class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:border-[var(--primary)] focus:ring-2 focus:ring-[var(--primary)]/20 outline-none transition-all"
          placeholder="deine@email.de"
        />
      </div>

      <div>
        <label for="message" class="block text-sm font-medium mb-2">Nachricht</label>
        <textarea
          id="message"
          name="message"
          rows="5"
          required
          class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:border-[var(--primary)] focus:ring-2 focus:ring-[var(--primary)]/20 outline-none transition-all resize-none"
          placeholder="Deine Nachricht..."
        ></textarea>
      </div>

      <button
        type="submit"
        class="btn btn-primary w-full"
      >
        Nachricht senden
      </button>

      <p class="text-xs text-[var(--text-muted)] text-center">
        Mit dem Absenden stimmst du der Verarbeitung deiner Daten gemäß unserer <a href="/datenschutz" class="underline">Datenschutzerklärung</a> zu.
      </p>
    </form>
  </div>
</div>
